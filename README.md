# react-native-mock-render-example

This package shows how to integrate react-native-mock into your React Native app.

To get started just run ```yarn``` and ```yarn:test``` - if the tests pass you're good to go.
